name: Bad Workflow

on:
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.10"

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Error
        run: |
          sleep 120
          exit 1
