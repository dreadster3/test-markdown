name: Render Markdown

on:
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.10"

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Checkout code
        uses: actions/checkout@v3
        repo: dreadster3/dynamic_markdown
        path: tools/

      - name: Set up Python ${{env.PYTHON_VERSION}}
        uses: actions/setup-python@v3
        with:
          python-version: ${{env.PYTHON_VERSION}}

      - name: Test
        run: |
          ls
          ls tools
